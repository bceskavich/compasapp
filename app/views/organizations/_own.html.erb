<%= form_for(current_user.owners.build(:organization_id => @organization.id, :user_id => session[:user_id]), :remote => true) do |f| %>
	<div><%= f.hidden_field :organization_id %></div>
	<div><%= f.hidden_field :user_id %></div>
	<%= f.submit "Add Admin!" %>
<% end %>