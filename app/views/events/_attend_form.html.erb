<% if current_user.attending?(@event, current_user) %>
	<%= render 'unattend' %>
    <script>
        $(function() {
            $('input').one('click',function(){
                console.log('clicked!');
                var count = parseInt($('.count').html())-1;
                console.log(count);
                $('.count').html(count);
            });
        });
    </script>
<% else %>
	<%= render 'attend' %>
    <script>
        $(function() {
            $('input').one('click',function(){
                console.log('clicked!');
                var count = parseInt($('.count').html())+1;
                console.log(count);
                $('.count').html(count);
            });
        });
    </script>
<% end %>